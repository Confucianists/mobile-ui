// 颜色定义规范
$color-background = #fff
$color-bg-hui = #F5F5F5
$color-hui = #cccccc
$color-b = #e1e1e4
$color-ash = #999999
$color-blue = #4242FF
$color-red = #FF4133
$color-theme = #ffcd32tip
$color-sub-theme = #d93f30
$color-text = #333
$color-pink = #ffa099
$color-hui-bg = #f3f5f5
$color-line = #e5e5e5

// 主要按钮颜色
$color-main-btn = rgb(29, 161, 242)
// 背景颜色
$color-background = #f8f8f8
// 细灰线颜色
$color-line-small = #e5e5e5


/*********cube-ui**********/
//vw
vw($pxsize)
  ($pxsize/750)*100vw

.banner .cube-slide-dots{
  bottom vw(64);
}
.banner .cube-slide-dots>span.active{
    background-color: #ffffff;
}
.cube-slide-dots > span{
    height 3px
}
.new-recommend .cube-slide-dots{
    right 0
    bottom: vw(300)
}
.xianshi .cube-slide-dots{
    display: none;
}

.cube-picker-panel {
    border-radius: vw(20) vw(20) 0 0;
}
.cube-dialog-main{
  border-radius: vw(24)
}
/************vue-transiton************/
.slide-enter-active {
    animation-name: slideInUp;
    animation-duration: 0.5s;
    animation-fill-mode: both;
}
.slide-leave-active {
    animation-name: slideOutDown;
    animation-duration: 0.5s;
    animation-fill-mode: both;
}
@keyframes slideInUp {
    0% {
        transform: translate3d(0, 100%, 0);
        visibility: visible;
    }

    to {
        transform: translateZ(0);
    }
}
@keyframes slideOutDown {
    0% {
        transform: translateZ(0);
    }

    to {
        visibility: hidden;
        transform: translate3d(0, 100%, 0);
    }
}
.delay-leave-active {
    -webkit-animation-delay: 0.2s;
    -moz-animation-delay: 0.2s;
    -o-animation-delay: 0.2s;
    animation-delay: 0.2s;
}

/**************slide-left slide-right****************/
.slide-right-enter-active,
.slide-right-leave-active,
.slide-left-enter-active,
.slide-left-leave-active {
  will-change: transform;
  transition: all 400ms;
  position: absolute;
}
.slide-right-enter {
  opacity: 0;
  transform: translate3d(-100%, 0, 0);
}
.slide-right-leave-active {
  opacity: 0;
  transform: translate3d(100%, 0, 0);
}
.slide-left-enter {
  opacity: 0;
  transform: translate3d(100%, 0, 0);
}
.slide-left-leave-active {
  opacity: 0;
  transform: translate3d(-100%, 0, 0);
}